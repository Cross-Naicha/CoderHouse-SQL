---
title: "ArkhamDB Documentation"
author: "Daruich, Nicolás Alberto"
date: "2024-01-22"
output:
  word_document: default
  html_document: default
---

# ArkhamDB

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

# Generales
card_id <- "Identificador único de la carta."
card_class <- "Clase (Guardían, Buscador, etc)."
card_name <- "Nombre de la carta."
card_traits <- "Rasgos propios de la carta."
card_health <- "Tolerancia fisica."
card_sanity <- "Tolerancia mental."
card_legend <- "Leyenda de interés narrativo."

chapter_id <- "Capítulo de la historia de la cual forma parte."

# Cartas de Jugador
card_cost <- "Costo requerido para jugar la carta en una partida."
card_level <- "Nivel de experiencia requerido para contar con la carta en el mazo."
card_type <- "Tipo de carta."

card_willpower <- "Iconos de voluntad asignables a una prueba."
card_intellect <- "Iconos de intelecto asignables a una prueba."
card_combat <- "Iconos de combate asignables a una prueba."
card_agility <- "Iconos de agilidad asignables a una prueba."
card_wild <- "Iconos comodín asignables a una prueba."

card_active <- "Capacidad activa de la carta."

card_slot <- "Espacio que ocupa el apoyo en el investigador."
```

## Tablas

### Tabla: Investigadores

#### [Breve descripción contextual]{.underline}
La carta de investigador es unica, y se asigna al jugador. La misma determina las caracteristicas de la construccion del mazo y, por lo tanto, de las posibilidades que dicho jugador tendra en la partida.

```{r investigators_table, echo = FALSE}
csv_path <- "Investigators.csv"
investigators_df <- read.csv(csv_path, sep = ";")

print(investigators_df)
```

#### [Descripcion de las columnas: Investigador]{.underline}

-   **card_id**: `r card_id`
-   **inv_class**: `r card_class`
-   **inv_name**: `r card_name`
-   **inv_subname**: Apodo del investigador.
-   **inv_traits**: `r card_traits` En este caso, rasgos del investigador.
-   **inv_health**: `r card_health`
-   **inv_sanity**: `r card_sanity`
-   **inv_willpower**: Voluntad del investigador.
-   **inv_intellect**: Inteligencia del investigador.
-   **inv_combat**: Habilidad para combatir del investigador.
-   **inv_agility**: Agilidad del investigador.
-   **inv_elder_sign_value**: Valor que toma el "elder_sign" si sale de la bolsa de caos.
-   **inv_elder_sign_effect**: Efecto ante la salida del "elder_sign" al salir de la bolsa de caos.
-   **inv_ability**: Habilidad única del investigador.
-   **inv_legend**: 
-   **inv_deck_size**: Tamaño del mazo requerido por el investigador.
-   **inv_deck_building**: Características requeridas para la construcción del mazo.
-   **inv_unique_cards**: Cartas únicas asociadas y requeridas para la construcción del mazo.
-   **chapter_id**: `r chapter_id`

## Cartas de Jugador

#### [Breve descripción contextual]{.underline}
Las cartas de jugador se engloban entre 3 categorías: apoyos, eventos y habilidades. Si bien tienen muchas características comúnes, cada tipo presenta sus particularidades.

### Tabla: Apoyos (Assets)

```{r assets_table, echo = FALSE}
csv_path <- "Assets.csv"
assets_df <- read.csv(csv_path, sep = ";")

print(assets_df)
```

#### [Descripcion de las columnas: Apoyos]{.underline}

-   **card_id**: `r card_id`
-   **card_cost**: `r card_cost`
-   **card_level**: `r card_level`
-   **card_class**: `r card_class`
-   **card_name**: `r card_name`
-   **card_phrase**: `r card_legend`
-   **card_traits**: `r card_traits`
-   **card_willpower**: `r card_willpower`
-   **card_intellect**: `r card_intellect`
-   **card_combat**: `r card_combat`
-   **card_agility**: `r card_agility`
-   **card_wild**: `r card_wild`
-   **card_health**: `r card_health`
-   **card_sanity**: `r card_sanity`
-   **card_passive**: Capacidad pasiva de la carta.
-   **card_active**: `r card_active`
-   **card_slot**: `r card_slot`
-   **chapter_id**: `r chapter_id`

### Tabla: Eventos (Events)

```{r events_table, echo = FALSE}
csv_path <- "Events.csv"
events_df <- read.csv(csv_path, sep = ";")

print(events_df)
```

#### [Descripcion de las columnas: Eventos]{.underline}

-   **card_id**: `r card_id`
-   **card_cost**: `r card_cost`
-   **card_level**: `r card_level`
-   **card_class**: `r card_class`
-   **card_name**: `r card_name`
-   **card_traits**: `r card_traits`
-   **card_willpower**: `r card_willpower`
-   **card_intellect**: `r card_intellect`
-   **card_combat**: `r card_combat`
-   **card_agility**: `r card_agility`
-   **card_wild**: `r card_wild`
-   **card_active**: `r card_active`
-   **card_legend**: `r card_legend`
-   **chapter_id**: `r chapter_id`

### Tabla: Habilidad (Skills)

```{r skills_table, echo = FALSE}
csv_path <- "Skills.csv"
skills_df <- read.csv(csv_path, sep = ";")

print(skills_df)
```

#### [Descripcion de las columnas: Habilidades]{.underline}

-   **card_id**: `r card_id`
-   **card_cost**: `r card_cost`
-   **card_level**: `r card_level`
-   **card_class**: `r card_class`
-   **card_name**: `r card_name`
-   **card_traits**: `r card_traits`
-   **card_willpower**: `r card_willpower`
-   **card_intellect**: `r card_intellect`
-   **card_combat**: `r card_combat`
-   **card_agility**: `r card_agility`
-   **card_wild**: `r card_wild`
-   **card_active**: `r card_active`
-   **card_legend**: `r card_legend`
-   **chapter_id**: `r chapter_id`

### Tabla: Antología (Anthology)

#### [Breve descripción contextual]{.underline}
El juego gira en torno a una colección de historias que no tienen necesariamente continuidad, pero que están todas basadas en el universo de Lovecraft. Cada historia se divide a su vez en capítulos que si guardan continuidad y que hacen al todo de la misma. Sin embargo, muchas historias se limitan a un único capitulo.

```{r anthology_table, echo = FALSE}
csv_path <- "anthologies.csv"
anthologies_df <- read.csv(csv_path, sep = ";")

print(anthologies_df)
```

#### [Descripcion de las columnas: Antologia]{.underline}

-   **ant_id**: Codigo único de la historia.
-   **ant_name**: Nombre de la historia. 
-   **ant_type**: Tipo de historia (extensa/único capitulo).
-   **ant_date**: Fecha de lanzamiento.

### Tabla: Capítulos (Chapters)

#### [Breve descripción contextual]{.underline}
Capítulos que forman parte de una historia extensa, o que hacen a una historia de un único capítulo.

```{r chapters_table, echo = FALSE}
csv_path <- "Chapters.csv"
chapters_df <- read.csv(csv_path, sep = ";")

print(chapters_df)
```

-   **chapter_id**: Código único de la historia.
-   **chapter_name**: Nombre del capítulo.
-   **ant_id**: Código único de la historia de la cúal el capítulo forma parte.

### Tabla: Jugadores (Players)

```{r players_table, echo = FALSE}
csv_path <- "players.csv"
players_df <- read.csv(csv_path, sep = ";")

print(players_df)
```

### Tabla: Campañas (Campaigns)

```{r campaigns_table, echo = FALSE}
csv_path <- "campaigns.csv"
campaigns_df <- read.csv(csv_path, sep = ";")

print(campaigns_df)
```

### Tabla: Equipos (Teams)

```{r teams_table, echo = FALSE}
csv_path <- "teams.csv"
teams_df <- read.csv(csv_path, sep = ";")

print(teams_df)
```

### Tabla: Mazos (Decks)

```{r decks_table, echo = FALSE}
csv_path <- "decks.csv"
decks_df <- read.csv(csv_path, sep = ";")

print(decks_df)
```
